# Base templates (Stage 6)

Canonical `AGENTS.md` and `CLAUDE.md` templates for **agentboot** land here in Stage 6.

Stage 5 keeps the existing machine baseline at `global/AGENTS.md` and per-repo overlay at `templates/AGENTS.md`. This directory is reserved for the richer agentboot template pack.
