# Deferred AgentOS features

Delivered in the slim bootstrap / Phases 0–2 and 5: curated skills
install/update, global AGENTS.md outputs, profile-driven `agentbot boot`
workspace rendering and registration, the standalone Agentbot menu, the
Dotfiles sibling bridge, and the optional Graphify CLI/assistant integration.

Still deferred:

- Full numbered taxonomy (`00-core/`, `10-rules/`, `80-adapters/`, …)
- TUI overhaul and `agentos tui`
- Full source-ingest pipeline (`agentos ingest`, diff/review/accept)
- **Hermes / opencode integration** (separate future project — laptop stays CLI-only; home server out of scope for this project)
- OpenClaw bootstrap file set (`SOUL.md`, `USER.md`, …)
- ChatGPT and additional export adapters
- Memory graph upgrades — Graphify hooks, Mem0, and Graphiti/GraphRAG
  (Phase 7.4; the standalone Graphify CLI integration is already live)
